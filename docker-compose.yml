version: "3.8"

services:
  nexus-node:
    build: .
    container_name: nexus-node
    restart: unless-stopped
    stdin_open: true
    tty: true
    environment:
      - NODE_ID=${NODE_ID}
    volumes:
      - nexus-data:/root/.nexus

volumes:
  nexus-data:
